const recaptcha = require ('./recaptcha');
const antiSpam = require ('./anti-inbox-spam');
const antiDualSubscriber = require ('./anti-dual-subscriber');

module.exports = { recaptcha, antiSpam, antiDualSubscriber}